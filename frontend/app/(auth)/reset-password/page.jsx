import Reset from "@/components/auth/Reset";

export const metadata = {
  title: "Footy Scouts | Reset Password",
  description: "Reset your Footy Scouts password",
};

export default function ResetPassword() {
  return <Reset />;
}
